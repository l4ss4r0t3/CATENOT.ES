// VARIÁVEIS PARA O PAN E ZOOM
let offsetX = 0;
let offsetY = 0;
let scale = 1;

// VARIÁVEIS PARA CONTAINER PRINCIPAL EO WORLD
const windowContainer = document.getElementById('window');
const worldContainer = document.getElementById('world');